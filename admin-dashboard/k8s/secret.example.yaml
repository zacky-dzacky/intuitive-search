# Copy to secret.yaml (git-ignored) and fill in real values before `kubectl apply`.
apiVersion: v1
kind: Secret
metadata:
  name: admin-dashboard-secret
  namespace: default
type: Opaque
stringData:
  DATABASE_URL: "postgresql://bank:bank@host.docker.internal:5432/banksearch"
  ADMIN_PASSWORD: "change-me"
  SESSION_SECRET: "replace-with-openssl-rand-hex-32"
