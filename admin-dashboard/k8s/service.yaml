apiVersion: v1
kind: Service
metadata:
  name: admin-dashboard-svc
  namespace: default
spec:
  selector:
    app: admin-dashboard
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
