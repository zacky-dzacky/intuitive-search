apiVersion: v1
kind: Service
metadata:
  name: embedding-service-svc
  namespace: default
spec:
  selector:
    app: embedding-service
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
